# Fleet SSL sweep — 2026-08-14
**Checked:** 195 (site 74 · game 60 · tool 61)
**Live:** 188 → 188 · **Still broken:** 7
**Cert budget:** ~36.2 of 50 per week. Property ceiling ~373.
## Registry freshness

- game — advanced 1 commit(s) — the sweep would have run stale

## Orphan DNS candidates left alone

- `conflictmap.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `lab.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `pagewell.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `www.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it

## On hold — deliberately not cycled

- metascrub (tool) `authorization_created` on `metascrub.benrichardson.dev` — 6d left, 08-13 canary protocol: castwell cycles alone on 08-15; these six wait for its result
- au-insolvency (site) `authorization_created` on `au-insolvency-tracker.benrichardson.dev` — 6d left, 08-13 canary protocol: castwell cycles alone on 08-15; these six wait for its result
- au-approvals (site) `authorization_created` on `au-build-approvals.benrichardson.dev` — 6d left, 08-13 canary protocol: castwell cycles alone on 08-15; these six wait for its result
- noisewell (tool) `authorization_created` on `noisewell.benrichardson.dev` — 6d left, 08-13 canary protocol: castwell cycles alone on 08-15; these six wait for its result
- au-inflation (site) `new` on `au-cpi-explorer.benrichardson.dev` — 6d left, 08-13 canary protocol: castwell cycles alone on 08-15; these six wait for its result
- facet (game) `new` on `facet-dice.benrichardson.dev` — 6d left, 08-13 canary protocol: castwell cycles alone on 08-15; these six wait for its result
- castwell (tool) `new` on `castwell-cast.benrichardson.dev` — 0d left, 08-06 finding: hostnames will not validate; cycling re-taints them

## HTTPS enforcement enabled

2 properties.

## Still broken

| Property | Cat | Class | cert_state | probe |
|---|---|---|---|---|
| au-inflation | site | A | `new` | tls |
| au-approvals | site | A | `authorization_created` | tls |
| au-insolvency | site | A | `authorization_created` | tls |
| facet | game | A | `new` | tls |
| castwell | tool | A | `new` | tls |
| noisewell | tool | A | `authorization_created` | tls |
| metascrub | tool | A | `authorization_created` | tls |

## The 08-14 re-probe — the result

This is the clean re-probe the 08-06 finding asked for and Cause 9 scheduled: eight days
untouched, nothing cycled, states read as they stand. The run cycled **zero** properties, as
predicted — it fired at `08-13T22:10Z`, eleven minutes before `castwell`'s hold lapsed.

**All seven are frozen.** Every `cert_state` is byte-identical to 08-13, and to 08-07 when the
holds were placed:

| Property | Hostname | state on 08-07 | state on 08-14 |
|---|---|---|---|
| au-inflation | `au-cpi-explorer` | `new` | `new` |
| au-approvals | `au-build-approvals` | `authorization_created` | `authorization_created` |
| au-insolvency | `au-insolvency-tracker` | `authorization_created` | `authorization_created` |
| facet | `facet-dice` | `new` | `new` |
| castwell | `castwell-cast` | `new` | `new` |
| noisewell | `noisewell` | `authorization_created` | `authorization_created` |
| metascrub | `metascrub` | `authorization_created` | `authorization_created` |

Per Cause 9 this does **not** settle the question, and the canary protocol stands unchanged.
A stalled `authorization_created` does not advance on its own — nothing asked these hostnames
for a certificate all week, so "frozen" is equally consistent with a permanent block and with
no request having been made. `castwell` cycles alone at the 08-15 08:10 run; its hold has
lapsed and the other six are held. Nothing needs to be remembered tomorrow.

## Verification checks (2026-08-14)

- **Budget measured an eighth day: 23 / 50** — `08-08:3 · 08-09:3 · 08-10:4 · 08-11:3 ·
  08-12:4 · 08-13:5 · 08-14:1`. Twenty-seven spare. The script's modelled `~36.2/50` is
  **thirteen certificates above** the measurement.
- **`au-intl-courses` issued this morning** on a fresh hostname while all seven stayed frozen.
  Eighth consecutive datapoint that issuance works and the backlog is hostname-scoped.
- **Cause 5 / takeovers: clean.** Registry host vs repo Pages `cname` compared across all 195
  properties — no mismatches. The zone sweep left four records alone, all four serving our own
  content by design (`conflictmap`, `lab`, `pagewell`, `www`).
- **No property is `approved` but unenforced** after this run's `+2`.
- **The no-certificate list holds no surprises:** the seven held, plus the three standing benign
  entries — `au-worksafe` and `huntress` (path-hosted) and `provenova` (external apex).
- **Zone at 208 / 3500.** No pressure; Cause 8 has no way to fire.
- **No factory shipped a stalled certificate**; the 08-02 poll fix is holding in all three.
  No factory shipped at all at 04:10 today, and none reached for the path-hosting fallback.
