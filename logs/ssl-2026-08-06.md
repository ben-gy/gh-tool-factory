# Fleet SSL sweep — 2026-08-06
**Checked:** 175 (site 66 · game 53 · tool 56)
**Live:** 168 → 168 · **Still broken:** 7
**Cert budget:** ~34.6 of 50 modelled; **37 / 50 measured** (see below). Property ceiling ~373.

Two passes. The 08:10 pass saw a catalog of 173 and reported `live 167 / broken 6`; both
figures were wrong. It counted `metascrub` live off a hijacked hostname (below), and
`gh-tool-factory` and `gh-game-factory` had not yet pushed `charsight` and `brood` when the
registries were read. The second pass ran against fresh registries with `--max-cycles=0`,
deliberately cycling nothing — see "The backlog is NOT waiting on certificate budget".

## HTTPS enforcement enabled

3 properties — `au-advisers` (08:10 pass), then `charsight` and `brood`, both of which
shipped `approved` but unenforced while the first pass was running.

## Still broken

| Property | Cat | Class | cert_state | probe |
|---|---|---|---|---|
| au-inflation | site | A | `new` | tls |
| au-approvals | site | A | `authorization_created` | tls |
| au-insolvency | site | A | `authorization_created` | tls |
| facet | game | A | `new` | tls |
| castwell | tool | A | `new` | tls |
| noisewell | tool | A | `authorization_created` | tls |
| metascrub | tool | A | `authorization_created` | tls |

## SECURITY — `metascrub-app.benrichardson.dev` has been taken over

**A stranger is serving Indonesian gambling spam from a `benrichardson.dev` subdomain, and
the tool registry was linking to it.**

`metascrub` was registered at `metascrub-app.benrichardson.dev`, but the repo's Pages
`cname` and its `public/CNAME` both say `metascrub.benrichardson.dev`. When the property
moved, the DNS record for the old hostname was left pointing at `ben-gy.github.io` with
nothing of ours claiming it — a dangling CNAME. Someone else claimed that hostname on their
own GitHub Pages repo and has been serving it since:

```
$ curl -s https://metascrub-app.benrichardson.dev/ | grep -o '<title>.*</title>'
<title>Tombolwd # Toto Slot 10K Pilihan Simpel Buat Cari Cuan Maksimal</title>

$ curl -sI https://metascrub-app.benrichardson.dev/
server: GitHub.com
last-modified: Mon, 27 Jul 2026 11:40:54 GMT

$ openssl s_client -connect metascrub-app.benrichardson.dev:443 …
subject= /CN=metascrub-app.benrichardson.dev
issuer=  /C=US/O=Let's Encrypt/CN=YR2
notBefore=Jul 15 14:12:37 2026 GMT
```

The certificate was issued **2026-07-15**, so the hostname has been in someone else's hands
for three weeks. Note that their certificate — and every renewal of it — is issued against
`benrichardson.dev` and therefore **spends from the fleet's own 50/week Let's Encrypt
bucket**.

**Not remediated by this run.** Removing the DNS record is the fix, but the standing
guardrail is to delete a record only when GitHub reports the hostname *unclaimed*, and this
one is claimed — by them. Deliberately deleting a record someone else is actively serving is
a decision for the operator, not for an unattended sweep. It is reversible and it is one
call:

```bash
CF=$(cat ~/.claude/secrets/cf-benrichardson-token); ZID=bf71cf2904d22db3443bb471e5dbdde2
ID=$(curl -s -H "Authorization: Bearer $CF" \
  "https://api.cloudflare.com/client/v4/zones/$ZID/dns_records?name=metascrub-app.benrichardson.dev" \
  | python3 -c 'import sys,json;print(json.load(sys.stdin)["result"][0]["id"])')
curl -s -X DELETE -H "Authorization: Bearer $CF" \
  "https://api.cloudflare.com/client/v4/zones/$ZID/dns_records/$ID"
```

What *was* done: the registry URL was corrected to `https://metascrub.benrichardson.dev`, so
the tool index no longer links to the spam. That reclassifies `metascrub` as broken — its
real hostname is sitting at `authorization_created` like the rest of the backlog.

### The script cannot currently see this class of failure

`--fix-registry` corrects a registry URL when the probe *fails*. A hijacked hostname returns
`200` over valid TLS, so the property scores as healthy and the mismatch is never examined.
The check that catches it is structural, not behavioural: **registry host ≠ the repo's Pages
`cname`**. Sweeping that comparison across all 175 properties found exactly one mismatch —
this one — so the fleet is otherwise clean.

The zone was also swept for the underlying condition — a record pointing at
`ben-gy.github.io` with no matching Pages `cname` anywhere in the fleet. Ten qualify:

| Hostname | Serves | Verdict |
|---|---|---|
| `conflictmap`, `pagewell`, `lab`, `www` | our content | outside the three registries by design — fine |
| `au-approvals`, `au-baby-names`, `au-inflation`, `au-ndis-watch`, `vic-rents` | GitHub 404 "Site not found" | **superseded hostnames, unclaimed — safe to prune, 5 records** |
| `metascrub-app` | someone else's spam | **taken over, see above** |

(`brood` and `charsight` appeared in this list on the first pass purely because their
registry entries had not been pushed yet. Any future run of this check must read the
registries fresh, or it will nominate the day's newest properties for deletion.)

The five unclaimed records are the same shape as the takeover that already happened — a
superseded hostname left pointing at GitHub Pages is exactly how `metascrub-app` was lost.
The zone stands at 195/200; pruning them doubles the remaining headroom to 10.

They were not deleted here because the 08-04 session explicitly logged the superseded
records as an operator decision. That decision is now also a security one.

## The backlog is NOT waiting on certificate budget

The 08-04 amendment's test — "if the bucket were exhausted, new properties would stall too"
— now has a direct measurement behind it rather than an inference. Deriving issuance dates
from `expires_at - 90d` across all 175 properties:

| Day | Certs issued |
|---|---|
| 2026-07-31 | 1 |
| 2026-08-01 | 3 |
| 2026-08-02 | 14 |
| 2026-08-03 | 3 |
| 2026-08-04 | 13 |
| 2026-08-05 | 2 |
| 2026-08-06 | 3 |
| **Trailing 7 days** | **37 / 50** |

**Thirteen tokens of headroom**, and three of them were spent this morning — `au-advisers`,
`charsight` and `brood` all went from shipped to `approved` in minutes. Meanwhile all six
backlog properties held an undisturbed authorization for a full 24h and not one state
advanced. `metascrub` makes seven, and it has been sitting at `authorization_created` far
longer.

That combination rules out the budget. It also rules out configuration: DNS for the five
fresh hostnames is byte-identical to the healthy controls (`CNAME` → `ben-gy.github.io` plus
the four A records, unproxied), all six serve `200` over plain HTTP so the HTTP-01 path is
reachable, and Pages reports `build_type: workflow`, `source main:/`, correct `cname`.

**What is left is that these six hostnames specifically will not complete validation.** Five
of them are *fresh* hostnames minted on 08-04, which is what makes this awkward: the 08-04
hypothesis was that the block is hostname-scoped and a fresh hostname clears it, and on that
evening eight fresh hostnames did issue within a minute. These five were minted the same way
in the same session and have not. The distinguishing fact is that these five are exactly the
ones the 08-05 08:12 run stomped — their first authorization was abandoned at ~9h old. So
the open question is whether abandoning an authorization taints the hostname it was opened
against.

If it does, the sequence is self-perpetuating: cycle → abandon → taint → cycle again, which
is the loop this routine has been running. Cycling these five again is therefore the action
most likely to be harmful, so **this run deliberately cycled nothing** (`--max-cycles=0`) and
the second pass reports them as deferred. Recommend leaving all seven strictly untouched for
a week and re-probing: it costs nothing, and it distinguishes "tainted, ages out" from
"permanently dead".

**Tomorrow's 08:10 run will cycle them unless something stops it** — they are long past the
cooldown gate now. Either pass `--max-cycles=0` again, or bump `--cooldown-hours` past the
intended quiet period (`--cooldown-hours=168` holds them for the week).

`noisewell` is the control: it is the one property never moved to a fresh hostname (the zone
was at 200/200 on 08-04), so it has been sitting on its original blocked hostname the whole
time. It behaves identically to the five fresh ones.

## Cooldown is effectively 48h, not 24h

Minor, but it halves the repair cadence. The sweep runs at 08:10; yesterday's cycles were
stamped 08:12 and 08:21. At 08:10 the next morning they are 23.98h old, miss the 24h gate by
two minutes, and wait a further full day. Worth trimming the gate to ~23h if cycling is ever
resumed — not changed here, because on the evidence above cycling is not what these
properties need.

## Factories

All three factories shipped today — `au-advisers` (site), `brood` (game), `charsight` (tool)
— and all three came out `approved`. No regression of the 08-02 poll fix.

Two of them did ship *unenforced*, and both had pushed their registry entry after the 08:10
sweep read it. That is a timing collision rather than a fault: a property that lands between
the factory run and the sweep gets neither the factory's enforcement step nor the sweep's.
It self-corrects the following morning; worth knowing when a property looks briefly
unenforced.

`au-worksafe` (08-04) is registered at `https://ben-gy.github.io/au-worksafe/` with no custom
domain and no certificate object at all. It serves fine on `github.io`, so the sweep scores
it live, but it is the first property to have silently fallen back off the fleet's domain —
the DNS record cap starting to show up in factory output.
