# Fleet SSL sweep — 2026-08-18
**Checked:** 204 (site 75 · game 64 · tool 65)
**Live:** 196 → 196 · **Still broken:** 8
**Cert budget:** ~36.9 of 50 per week. Property ceiling ~373.
## Orphan DNS candidates left alone

- `conflictmap.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `lab.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `pagewell.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `www.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it

## On hold — deliberately not cycled

- metascrub (tool) `authorization_created` on `metascrub.benrichardson.dev` — 4d left, 08-17: the fresh-hostname escape is NO LONGER PROVEN — crowdsize (minted 08-15, outside the 08-04 batch) is still cert=new 36h on and 10h after a solo cycle. GitHub's own pages/health reports all eight is_valid + https_eligible + CAA-clean + served_by_pages, and the budget measured 22/50. Do NOT mass-cycle, and do NOT start the seven-property rename until the crowdsize case is understood.
- au-insolvency (site) `authorization_created` on `au-insolvency-tracker.benrichardson.dev` — 4d left, 08-17: the fresh-hostname escape is NO LONGER PROVEN — crowdsize (minted 08-15, outside the 08-04 batch) is still cert=new 36h on and 10h after a solo cycle. GitHub's own pages/health reports all eight is_valid + https_eligible + CAA-clean + served_by_pages, and the budget measured 22/50. Do NOT mass-cycle, and do NOT start the seven-property rename until the crowdsize case is understood.
- au-approvals (site) `authorization_created` on `au-build-approvals.benrichardson.dev` — 4d left, 08-17: the fresh-hostname escape is NO LONGER PROVEN — crowdsize (minted 08-15, outside the 08-04 batch) is still cert=new 36h on and 10h after a solo cycle. GitHub's own pages/health reports all eight is_valid + https_eligible + CAA-clean + served_by_pages, and the budget measured 22/50. Do NOT mass-cycle, and do NOT start the seven-property rename until the crowdsize case is understood.
- noisewell (tool) `authorization_created` on `noisewell.benrichardson.dev` — 4d left, 08-17: the fresh-hostname escape is NO LONGER PROVEN — crowdsize (minted 08-15, outside the 08-04 batch) is still cert=new 36h on and 10h after a solo cycle. GitHub's own pages/health reports all eight is_valid + https_eligible + CAA-clean + served_by_pages, and the budget measured 22/50. Do NOT mass-cycle, and do NOT start the seven-property rename until the crowdsize case is understood.
- au-inflation (site) `new` on `au-cpi-explorer.benrichardson.dev` — 4d left, 08-17: the fresh-hostname escape is NO LONGER PROVEN — crowdsize (minted 08-15, outside the 08-04 batch) is still cert=new 36h on and 10h after a solo cycle. GitHub's own pages/health reports all eight is_valid + https_eligible + CAA-clean + served_by_pages, and the budget measured 22/50. Do NOT mass-cycle, and do NOT start the seven-property rename until the crowdsize case is understood.
- facet (game) `new` on `facet-dice.benrichardson.dev` — 4d left, 08-17: the fresh-hostname escape is NO LONGER PROVEN — crowdsize (minted 08-15, outside the 08-04 batch) is still cert=new 36h on and 10h after a solo cycle. GitHub's own pages/health reports all eight is_valid + https_eligible + CAA-clean + served_by_pages, and the budget measured 22/50. Do NOT mass-cycle, and do NOT start the seven-property rename until the crowdsize case is understood.
- castwell (tool) `new` on `castwell-cast.benrichardson.dev` — 4d left, 08-17: the fresh-hostname escape is NO LONGER PROVEN — crowdsize (minted 08-15, outside the 08-04 batch) is still cert=new 36h on and 10h after a solo cycle. GitHub's own pages/health reports all eight is_valid + https_eligible + CAA-clean + served_by_pages, and the budget measured 22/50. Do NOT mass-cycle, and do NOT start the seven-property rename until the crowdsize case is understood.
- crowdsize (tool) `new` on `crowdsize.benrichardson.dev` — 0d left, 08-17 re-probe: STILL cert=new — 36h after the hostname was minted and 10h after the 08-16 solo cycle. This is the 'still frozen tomorrow' condition the 08-16 hold was set to test: a second independent hostname stalling while sharing nothing with the 08-04 batch. DNS, CNAME file, build type and GitHub pages/health are all clean and match au-spectrum/epochcost, which issued. Hold lapses 08-18 so normal repair (one cycle) resumes on the 08-19 run.

## Still broken

| Property | Cat | Class | cert_state | probe |
|---|---|---|---|---|
| au-inflation | site | A | `new` | tls |
| au-approvals | site | A | `authorization_created` | tls |
| au-insolvency | site | A | `authorization_created` | tls |
| facet | game | A | `new` | tls |
| crowdsize | tool | A | `new` | tls |
| castwell | tool | A | `new` | tls |
| noisewell | tool | A | `authorization_created` | tls |
| metascrub | tool | A | `authorization_created` | tls |
