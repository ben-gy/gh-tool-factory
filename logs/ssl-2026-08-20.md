# Fleet SSL sweep — 2026-08-20
**Checked:** 209 (site 77 · game 66 · tool 66)
**Live:** 201 → 201 · **Still broken:** 8
**Cert budget:** ~37.3 of 50 per week. Property ceiling ~373.
## Registry freshness

- game — advanced 1 commit(s) — the sweep would have run stale; parked 1 untracked file(s) that collided with the incoming commit (logs/2026-08-19-standoff.md → *.superseded)
- tool — advanced 1 commit(s) — the sweep would have run stale

## Orphan DNS candidates left alone

- `conflictmap.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `lab.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `pagewell.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `www.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it

## On hold — deliberately not cycled

- metascrub (tool) `authorization_created` on `metascrub.benrichardson.dev` — 2d left, 08-17: the fresh-hostname escape is NO LONGER PROVEN — crowdsize (minted 08-15, outside the 08-04 batch) is still cert=new 36h on and 10h after a solo cycle. GitHub's own pages/health reports all eight is_valid + https_eligible + CAA-clean + served_by_pages, and the budget measured 22/50. Do NOT mass-cycle, and do NOT start the seven-property rename until the crowdsize case is understood.
- au-insolvency (site) `authorization_created` on `au-insolvency-tracker.benrichardson.dev` — 2d left, 08-17: the fresh-hostname escape is NO LONGER PROVEN — crowdsize (minted 08-15, outside the 08-04 batch) is still cert=new 36h on and 10h after a solo cycle. GitHub's own pages/health reports all eight is_valid + https_eligible + CAA-clean + served_by_pages, and the budget measured 22/50. Do NOT mass-cycle, and do NOT start the seven-property rename until the crowdsize case is understood.
- au-approvals (site) `authorization_created` on `au-build-approvals.benrichardson.dev` — 2d left, 08-17: the fresh-hostname escape is NO LONGER PROVEN — crowdsize (minted 08-15, outside the 08-04 batch) is still cert=new 36h on and 10h after a solo cycle. GitHub's own pages/health reports all eight is_valid + https_eligible + CAA-clean + served_by_pages, and the budget measured 22/50. Do NOT mass-cycle, and do NOT start the seven-property rename until the crowdsize case is understood.
- noisewell (tool) `authorization_created` on `noisewell.benrichardson.dev` — 2d left, 08-17: the fresh-hostname escape is NO LONGER PROVEN — crowdsize (minted 08-15, outside the 08-04 batch) is still cert=new 36h on and 10h after a solo cycle. GitHub's own pages/health reports all eight is_valid + https_eligible + CAA-clean + served_by_pages, and the budget measured 22/50. Do NOT mass-cycle, and do NOT start the seven-property rename until the crowdsize case is understood.
- au-inflation (site) `new` on `au-cpi-explorer.benrichardson.dev` — 2d left, 08-17: the fresh-hostname escape is NO LONGER PROVEN — crowdsize (minted 08-15, outside the 08-04 batch) is still cert=new 36h on and 10h after a solo cycle. GitHub's own pages/health reports all eight is_valid + https_eligible + CAA-clean + served_by_pages, and the budget measured 22/50. Do NOT mass-cycle, and do NOT start the seven-property rename until the crowdsize case is understood.
- facet (game) `new` on `facet-dice.benrichardson.dev` — 2d left, 08-17: the fresh-hostname escape is NO LONGER PROVEN — crowdsize (minted 08-15, outside the 08-04 batch) is still cert=new 36h on and 10h after a solo cycle. GitHub's own pages/health reports all eight is_valid + https_eligible + CAA-clean + served_by_pages, and the budget measured 22/50. Do NOT mass-cycle, and do NOT start the seven-property rename until the crowdsize case is understood.
- castwell (tool) `new` on `castwell-cast.benrichardson.dev` — 2d left, 08-17: the fresh-hostname escape is NO LONGER PROVEN — crowdsize (minted 08-15, outside the 08-04 batch) is still cert=new 36h on and 10h after a solo cycle. GitHub's own pages/health reports all eight is_valid + https_eligible + CAA-clean + served_by_pages, and the budget measured 22/50. Do NOT mass-cycle, and do NOT start the seven-property rename until the crowdsize case is understood.

## HTTPS enforcement enabled

3 properties.

## Still broken

| Property | Cat | Class | cert_state | probe |
|---|---|---|---|---|
| au-inflation | site | A | `new` | tls |
| au-approvals | site | A | `authorization_created` | tls |
| au-insolvency | site | A | `authorization_created` | tls |
| facet | game | A | `new` | tls |
| crowdsize | tool | A | `new` | tls |
| castwell | tool | A | `new` | tls |
| noisewell | tool | A | `authorization_created` | tls |
| metascrub | tool | A | `authorization_created` | tls |

## Post-run investigation (Fleet SSL Medic, 08-20)

No property was cycled today (all eight were held) and nothing moved. Verified independently of the
script:

- **All eight still stalled, unchanged from 08-19** — four `new`, four `authorization_created`,
  all failing TLS on the wire.
- **Issuance is working on the same registered domain today.** `topicgap`, `standoff` and `riser`
  shipped overnight and are all `approved` + `https_enforced`, as are `evalspill` and `deepshaft`.
  Three certificates issued this morning while the eight stayed frozen — the 08-19 control pair
  reproducing daily.
- **Budget measured 25 / 50** (5·3·3·3·2·2·4·3 across 08-13…08-20). Twenty-five spare, fourteenth
  consecutive day the budget is not the constraint. The `~37.3/50` model line is twelve above the
  measurement and is not a budget reading.
- **No factory shipped a stalled certificate**; the 08-02 poll fix is holding in all three.
- **Cause 10 did not repeat** — both game builds since the fix staged `registry.json`.
- The parked `logs/2026-08-19-standoff.md.superseded` was verified byte-identical to the committed
  copy and removed.

### Holds extended to 2026-09-03 — this was today's real action

All eight were held only to `2026-08-21T22:18Z`, and the 08-22 run fires `08-21T22:10Z` — eight
minutes inside that boundary. Any run starting slightly late would have mass-cycled six of the eight
(`--max-cycles=6`), spending six certificates on a failure that Cause 8 established on 08-19 cannot
respond to cycling. All eight are re-held for **14 days**, carrying the Cause 8 conclusion in the
hold reason so the next unattended run cannot re-derive its way past it. Detection is unaffected: if
GitHub picks the requests up, the state advances on its own and the daily sweep sees it.

Hold-run log: `ssl-2026-08-20-hold.md`. Full write-up in `ssl-root-cause-analysis.md`.

**Escalation to GitHub Support remains open and remains the operator's decision.**
