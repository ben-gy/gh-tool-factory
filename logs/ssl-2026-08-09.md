# Fleet SSL sweep — 2026-08-09
**Checked:** 183 (site 69 · game 56 · tool 58)
**Live:** 176 → 176 · **Still broken:** 7
**Cert budget:** ~35.2 of 50 per week. Property ceiling ~373.
## Orphan DNS candidates left alone

- `conflictmap.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `lab.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `metascrub-app.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `pagewell.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `www.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it

## On hold — deliberately not cycled

- metascrub (tool) `authorization_created` on `metascrub.benrichardson.dev` — 5d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- au-insolvency (site) `authorization_created` on `au-insolvency-tracker.benrichardson.dev` — 5d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- au-approvals (site) `authorization_created` on `au-build-approvals.benrichardson.dev` — 5d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- noisewell (tool) `authorization_created` on `noisewell.benrichardson.dev` — 5d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- au-inflation (site) `new` on `au-cpi-explorer.benrichardson.dev` — 5d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- facet (game) `new` on `facet-dice.benrichardson.dev` — 5d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- castwell (tool) `new` on `castwell-cast.benrichardson.dev` — 5d left, 08-06 finding: hostnames will not validate; cycling re-taints them

## Still broken

| Property | Cat | Class | cert_state | probe |
|---|---|---|---|---|
| au-inflation | site | A | `new` | tls |
| au-approvals | site | A | `authorization_created` | tls |
| au-insolvency | site | A | `authorization_created` | tls |
| facet | game | A | `new` | tls |
| castwell | tool | A | `new` | tls |
| noisewell | tool | A | `authorization_created` | tls |
| metascrub | tool | A | `authorization_created` | tls |
