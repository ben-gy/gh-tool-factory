# Fleet SSL sweep — 2026-08-08
**Checked:** 178 (site 68 · game 54 · tool 56)
**Live:** 171 → 171 · **Still broken:** 7
**Cert budget:** ~34.8 of 50 per week. Property ceiling ~373.
## Orphan DNS candidates left alone

- `conflictmap.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `lab.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `leakmap.benrichardson.dev` — record younger than the 48h grace window
- `metascrub-app.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `pagewell.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `sparklight.benrichardson.dev` — record younger than the 48h grace window
- `www.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it

## On hold — deliberately not cycled

- metascrub (tool) `authorization_created` on `metascrub.benrichardson.dev` — 6d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- au-insolvency (site) `authorization_created` on `au-insolvency-tracker.benrichardson.dev` — 6d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- au-approvals (site) `authorization_created` on `au-build-approvals.benrichardson.dev` — 6d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- noisewell (tool) `authorization_created` on `noisewell.benrichardson.dev` — 6d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- au-inflation (site) `new` on `au-cpi-explorer.benrichardson.dev` — 6d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- facet (game) `new` on `facet-dice.benrichardson.dev` — 6d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- castwell (tool) `new` on `castwell-cast.benrichardson.dev` — 6d left, 08-06 finding: hostnames will not validate; cycling re-taints them

## HTTPS enforcement enabled

2 properties.

## Still broken

| Property | Cat | Class | cert_state | probe |
|---|---|---|---|---|
| au-inflation | site | A | `new` | tls |
| au-approvals | site | A | `authorization_created` | tls |
| au-insolvency | site | A | `authorization_created` | tls |
| facet | game | A | `new` | tls |
| castwell | tool | A | `new` | tls |
| noisewell | tool | A | `authorization_created` | tls |
| metascrub | tool | A | `authorization_created` | tls |
