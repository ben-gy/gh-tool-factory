# Fleet SSL sweep — 2026-08-15
**Checked:** 197 (site 74 · game 61 · tool 62)
**Live:** 190 → 190 · **Still broken:** 7
**Cert budget:** ~36.3 of 50 per week. Property ceiling ~373.
## Registry freshness

- tool — advanced 1 commit(s) — the sweep would have run stale

## Certs cycled

- castwell (tool) — was `new`

## Orphan DNS candidates left alone

- `conflictmap.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `lab.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `pagewell.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `www.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it

## On hold — deliberately not cycled

- metascrub (tool) `authorization_created` on `metascrub.benrichardson.dev` — 5d left, 08-13 canary protocol: castwell cycles alone on 08-15; these six wait for its result
- au-insolvency (site) `authorization_created` on `au-insolvency-tracker.benrichardson.dev` — 5d left, 08-13 canary protocol: castwell cycles alone on 08-15; these six wait for its result
- au-approvals (site) `authorization_created` on `au-build-approvals.benrichardson.dev` — 5d left, 08-13 canary protocol: castwell cycles alone on 08-15; these six wait for its result
- noisewell (tool) `authorization_created` on `noisewell.benrichardson.dev` — 5d left, 08-13 canary protocol: castwell cycles alone on 08-15; these six wait for its result
- au-inflation (site) `new` on `au-cpi-explorer.benrichardson.dev` — 5d left, 08-13 canary protocol: castwell cycles alone on 08-15; these six wait for its result
- facet (game) `new` on `facet-dice.benrichardson.dev` — 5d left, 08-13 canary protocol: castwell cycles alone on 08-15; these six wait for its result

## HTTPS enforcement enabled

2 properties.

## Never issued (authorization undisturbed for a full cooldown)

- castwell (tool) `new` — 240h since cycle

## Still broken

| Property | Cat | Class | cert_state | probe |
|---|---|---|---|---|
| au-inflation | site | A | `new` | tls |
| au-approvals | site | A | `authorization_created` | tls |
| au-insolvency | site | A | `authorization_created` | tls |
| facet | game | A | `new` | tls |
| castwell | tool | A | `new` | tls |
| noisewell | tool | A | `authorization_created` | tls |
| metascrub | tool | A | `authorization_created` | tls |
