# Fleet SSL sweep — 2026-08-11
**Checked:** 188 (site 71 · game 57 · tool 60)
**Live:** 181 → 181 · **Still broken:** 7
**Cert budget:** ~35.6 of 50 per week. Property ceiling ~373.
## Registry freshness

- site — advanced 1 commit(s) — the sweep would have run stale
- tool — advanced 1 commit(s) — the sweep would have run stale

## Orphan DNS candidates left alone

- `conflictmap.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `lab.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `pagewell.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `www.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it

## On hold — deliberately not cycled

- metascrub (tool) `authorization_created` on `metascrub.benrichardson.dev` — 3d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- au-insolvency (site) `authorization_created` on `au-insolvency-tracker.benrichardson.dev` — 3d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- au-approvals (site) `authorization_created` on `au-build-approvals.benrichardson.dev` — 3d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- noisewell (tool) `authorization_created` on `noisewell.benrichardson.dev` — 3d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- facet (game) `new` on `facet-dice.benrichardson.dev` — 3d left, 08-06 finding: hostnames will not validate; cycling re-taints them
- castwell (tool) `new` on `castwell-cast.benrichardson.dev` — 3d left, 08-06 finding: hostnames will not validate; cycling re-taints them

## HTTPS enforcement enabled

1 properties.

## Still broken

| Property | Cat | Class | cert_state | probe |
|---|---|---|---|---|
| au-inflation | site | B | `new` | dns |
| au-approvals | site | A | `authorization_created` | tls |
| au-insolvency | site | A | `authorization_created` | tls |
| facet | game | A | `new` | tls |
| castwell | tool | A | `new` | tls |
| noisewell | tool | A | `authorization_created` | tls |
| metascrub | tool | A | `authorization_created` | tls |
