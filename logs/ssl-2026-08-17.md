# Fleet SSL sweep — 2026-08-17
**Checked:** 202 (site 75 · game 63 · tool 64)
**Live:** 194 → 194 · **Still broken:** 8
**Cert budget:** ~36.7 of 50 per week. Property ceiling ~373.
## Registry freshness

- tool — advanced 1 commit(s) — the sweep would have run stale

## Orphan DNS candidates left alone

- `conflictmap.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `lab.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `pagewell.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `www.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it

## On hold — deliberately not cycled

- metascrub (tool) `authorization_created` on `metascrub.benrichardson.dev` — 5d left, 08-15 canary result: castwell was cycled alone and did not move off 'new' in 36min (healthy hostnames issue in minutes). Cycling these hostnames looks futile; do NOT mass-cycle. Awaiting operator decision on fresh hostnames.
- au-insolvency (site) `authorization_created` on `au-insolvency-tracker.benrichardson.dev` — 5d left, 08-15 canary result: castwell was cycled alone and did not move off 'new' in 36min (healthy hostnames issue in minutes). Cycling these hostnames looks futile; do NOT mass-cycle. Awaiting operator decision on fresh hostnames.
- au-approvals (site) `authorization_created` on `au-build-approvals.benrichardson.dev` — 5d left, 08-15 canary result: castwell was cycled alone and did not move off 'new' in 36min (healthy hostnames issue in minutes). Cycling these hostnames looks futile; do NOT mass-cycle. Awaiting operator decision on fresh hostnames.
- noisewell (tool) `authorization_created` on `noisewell.benrichardson.dev` — 5d left, 08-15 canary result: castwell was cycled alone and did not move off 'new' in 36min (healthy hostnames issue in minutes). Cycling these hostnames looks futile; do NOT mass-cycle. Awaiting operator decision on fresh hostnames.
- au-inflation (site) `new` on `au-cpi-explorer.benrichardson.dev` — 5d left, 08-15 canary result: castwell was cycled alone and did not move off 'new' in 36min (healthy hostnames issue in minutes). Cycling these hostnames looks futile; do NOT mass-cycle. Awaiting operator decision on fresh hostnames.
- facet (game) `new` on `facet-dice.benrichardson.dev` — 5d left, 08-15 canary result: castwell was cycled alone and did not move off 'new' in 36min (healthy hostnames issue in minutes). Cycling these hostnames looks futile; do NOT mass-cycle. Awaiting operator decision on fresh hostnames.
- castwell (tool) `new` on `castwell-cast.benrichardson.dev` — 5d left, 08-15 canary result: castwell was cycled alone and did not move off 'new' in 36min (healthy hostnames issue in minutes). Cycling these hostnames looks futile; do NOT mass-cycle. Awaiting operator decision on fresh hostnames.
- crowdsize (tool) `new` on `crowdsize.benrichardson.dev` — 1d left, Fresh hostname minted 08-15T19:29Z, stalled at cert=new since creation; cycled alone 08-16 22:10Z. The 08-17 run fires at almost exactly the 24h cooldown boundary and would abandon that authorization (same trap as castwell on 08-15). Config verified identical to au-spectrum, which shipped 5h earlier the same night and issued. Lapses 08-18 so normal repair resumes on its own.

## HTTPS enforcement enabled

2 properties.

## Still broken

| Property | Cat | Class | cert_state | probe |
|---|---|---|---|---|
| au-inflation | site | A | `new` | tls |
| au-approvals | site | A | `authorization_created` | tls |
| au-insolvency | site | A | `authorization_created` | tls |
| facet | game | A | `new` | tls |
| crowdsize | tool | A | `new` | tls |
| castwell | tool | A | `new` | tls |
| noisewell | tool | A | `authorization_created` | tls |
| metascrub | tool | A | `authorization_created` | tls |

## Post-run investigation (Fleet SSL Medic, 08-17)

The eight held properties were investigated rather than accepted, because the 08-16 log set an
explicit condition on `crowdsize` for this morning. Full write-up in `ssl-root-cause-analysis.md`
under *"`crowdsize` is still frozen on 2026-08-17"*.

**Headline: `crowdsize` is still `cert=new`** — 36h after minting, 10h after its 08-16 solo cycle.
That is the "still frozen tomorrow" branch, so the 08-15 recommendation to rename the seven held
properties to fresh hostnames is **on hold, not cancelled**.

- **Budget measured: 22 / 50** (3 · 4 · 5 · 3 · 3 · 3 · 1 across 08-11…08-17), 28 spare. Eleventh
  consecutive day the budget is not the constraint. The modelled `~36.7/50` line above is 15 high.
- **New check — `GET /repos/:owner/:repo/pages/health`:** all eight stalled properties return
  `is_valid: true`, `is_https_eligible: true`, `caa_error: null`, `is_served_by_pages: true`,
  `reason: null`. The only negative is `https_error: peer_failed_verification`, which is the
  symptom of having no certificate. GitHub considers all eight eligible and never advances the
  request.
- **DNS re-verified** for all eight against four issuing controls (`epochcost`, `realmfold`,
  `au-spectrum`, `torc`): identical `CNAME → ben-gy.github.io`, unproxied, ttl 1, and **no duplicate
  or conflicting records** on any stalled name.
- **The "08-04 bad batch" framing is imprecise:** the eight stalled hostnames were created on four
  different dates spanning 06-26 to 08-15. Only five are 08-04 properties.
- **Controls:** `epochcost` and `realmfold` both shipped overnight and are `approved` + enforced.
  No factory shipped a stalled certificate; the 08-02 poll fix is holding in all three.

### Holds rewritten after this run

The hold reasons printed above are the ones this run read; they were rewritten afterwards so the
08-18 run reads today's finding instead of the superseded rename recommendation.

| Property | New expiry | Reason carried forward |
|---|---|---|
| the seven | `2026-08-21T22:18Z` | fresh-hostname escape no longer proven; do not mass-cycle, do not start the rename |
| `crowdsize` | `2026-08-17T22:18Z` | still frozen on re-probe; lapses so normal repair cycles it once on the 08-19 run |

The 08-18 run fires `08-17T22:10Z`, 8 minutes before `crowdsize`'s hold lapses — so it should
report it held at `0d left` and the cycle lands 08-19. If that run starts more than 8 minutes late
it will cycle a day early instead. Either way it is one certificate out of 28 spare and is the
intended next step.
