# Fleet SSL sweep — 2026-08-03
**Checked:** 164 (site 63 · game 49 · tool 52)
**Live:** 149 → 149 · **Still broken:** 15
**Cert budget:** ~33.8 of 50 per week. Property ceiling ~373.
## Still broken

| Property | Cat | Class | cert_state | probe |
|---|---|---|---|---|
| vic-rents | site | A | `authorization_created` | tls |
| au-inflation | site | A | `new` | tls |
| au-baby-names | site | A | `authorization_created` | tls |
| au-approvals | site | A | `new` | tls |
| au-insolvency | site | A | `new` | tls |
| au-ndis-watch | site | A | `authorization_created` | tls |
| skein | game | A | `authorization_created` | tls |
| facet | game | A | `new` | tls |
| emberwake | game | A | `new` | tls |
| tiltwell | tool | A | `authorization_created` | tls |
| castwell | tool | A | `new` | tls |
| wavewell | tool | A | `new` | tls |
| noisewell | tool | A | `authorization_created` | tls |
| veilpix | tool | A | `new` | tls |
| bgwipe | tool | A | `authorization_created` | tls |

## Notes

`https_enforced` was enabled on 9 properties during this morning's first pass (the 9 new
properties shipped since the 2026-08-02 sweep). The pass was repeated after fixing the
log-date bug below, by which point there was nothing left to enforce — hence no
enforcement section above.

The first pass also wrote its report over `ssl-2026-08-02.md`: `fleet-ssl.mjs` derived the
log date from `toISOString()` (UTC), which in AEST is still the previous day at 08:10.
Yesterday's log was restored from git and the script now uses the local date
(`lab@scripts/ssl/fleet-ssl.mjs`).
