# Fleet SSL sweep — 2026-08-20
**Checked:** 209 (site 77 · game 66 · tool 66)
**Live:** 201 → 201 · **Still broken:** 8
**Cert budget:** ~37.3 of 50 per week. Property ceiling ~373.
## Orphan DNS candidates left alone

- `conflictmap.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `lab.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `pagewell.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it
- `www.benrichardson.dev` — GitHub does not report it unclaimed — something is serving it

## On hold — deliberately not cycled

- metascrub (tool) `authorization_created` on `metascrub.benrichardson.dev` — 14d left, 08-20: CYCLING IS PROVEN FUTILE — Cause 8 established 08-19 that the stall is GitHub-side and per-property (crowdsize/torc/au-spectrum all minted 08-15: two issued in minutes, crowdsize never started; every layer we own is byte-identical to issuing controls). crowdsize then sat 72h undisturbed and never issued. Budget measured 25/50 on 08-20 with 3 certs issued that morning, so this is not throttling. Next step is a GitHub Support escalation and that is the operator's decision, not this routine's. Do NOT cycle, do NOT mass-cycle, do NOT rename.
- au-insolvency (site) `authorization_created` on `au-insolvency-tracker.benrichardson.dev` — 14d left, 08-20: CYCLING IS PROVEN FUTILE — Cause 8 established 08-19 that the stall is GitHub-side and per-property (crowdsize/torc/au-spectrum all minted 08-15: two issued in minutes, crowdsize never started; every layer we own is byte-identical to issuing controls). crowdsize then sat 72h undisturbed and never issued. Budget measured 25/50 on 08-20 with 3 certs issued that morning, so this is not throttling. Next step is a GitHub Support escalation and that is the operator's decision, not this routine's. Do NOT cycle, do NOT mass-cycle, do NOT rename.
- au-approvals (site) `authorization_created` on `au-build-approvals.benrichardson.dev` — 14d left, 08-20: CYCLING IS PROVEN FUTILE — Cause 8 established 08-19 that the stall is GitHub-side and per-property (crowdsize/torc/au-spectrum all minted 08-15: two issued in minutes, crowdsize never started; every layer we own is byte-identical to issuing controls). crowdsize then sat 72h undisturbed and never issued. Budget measured 25/50 on 08-20 with 3 certs issued that morning, so this is not throttling. Next step is a GitHub Support escalation and that is the operator's decision, not this routine's. Do NOT cycle, do NOT mass-cycle, do NOT rename.
- noisewell (tool) `authorization_created` on `noisewell.benrichardson.dev` — 14d left, 08-20: CYCLING IS PROVEN FUTILE — Cause 8 established 08-19 that the stall is GitHub-side and per-property (crowdsize/torc/au-spectrum all minted 08-15: two issued in minutes, crowdsize never started; every layer we own is byte-identical to issuing controls). crowdsize then sat 72h undisturbed and never issued. Budget measured 25/50 on 08-20 with 3 certs issued that morning, so this is not throttling. Next step is a GitHub Support escalation and that is the operator's decision, not this routine's. Do NOT cycle, do NOT mass-cycle, do NOT rename.
- au-inflation (site) `new` on `au-cpi-explorer.benrichardson.dev` — 14d left, 08-20: CYCLING IS PROVEN FUTILE — Cause 8 established 08-19 that the stall is GitHub-side and per-property (crowdsize/torc/au-spectrum all minted 08-15: two issued in minutes, crowdsize never started; every layer we own is byte-identical to issuing controls). crowdsize then sat 72h undisturbed and never issued. Budget measured 25/50 on 08-20 with 3 certs issued that morning, so this is not throttling. Next step is a GitHub Support escalation and that is the operator's decision, not this routine's. Do NOT cycle, do NOT mass-cycle, do NOT rename.
- facet (game) `new` on `facet-dice.benrichardson.dev` — 14d left, 08-20: CYCLING IS PROVEN FUTILE — Cause 8 established 08-19 that the stall is GitHub-side and per-property (crowdsize/torc/au-spectrum all minted 08-15: two issued in minutes, crowdsize never started; every layer we own is byte-identical to issuing controls). crowdsize then sat 72h undisturbed and never issued. Budget measured 25/50 on 08-20 with 3 certs issued that morning, so this is not throttling. Next step is a GitHub Support escalation and that is the operator's decision, not this routine's. Do NOT cycle, do NOT mass-cycle, do NOT rename.
- castwell (tool) `new` on `castwell-cast.benrichardson.dev` — 14d left, 08-20: CYCLING IS PROVEN FUTILE — Cause 8 established 08-19 that the stall is GitHub-side and per-property (crowdsize/torc/au-spectrum all minted 08-15: two issued in minutes, crowdsize never started; every layer we own is byte-identical to issuing controls). crowdsize then sat 72h undisturbed and never issued. Budget measured 25/50 on 08-20 with 3 certs issued that morning, so this is not throttling. Next step is a GitHub Support escalation and that is the operator's decision, not this routine's. Do NOT cycle, do NOT mass-cycle, do NOT rename.
- crowdsize (tool) `new` on `crowdsize.benrichardson.dev` — 14d left, 08-20: CYCLING IS PROVEN FUTILE — Cause 8 established 08-19 that the stall is GitHub-side and per-property (crowdsize/torc/au-spectrum all minted 08-15: two issued in minutes, crowdsize never started; every layer we own is byte-identical to issuing controls). crowdsize then sat 72h undisturbed and never issued. Budget measured 25/50 on 08-20 with 3 certs issued that morning, so this is not throttling. Next step is a GitHub Support escalation and that is the operator's decision, not this routine's. Do NOT cycle, do NOT mass-cycle, do NOT rename.

## Still broken

| Property | Cat | Class | cert_state | probe |
|---|---|---|---|---|
| au-inflation | site | A | `new` | tls |
| au-approvals | site | A | `authorization_created` | tls |
| au-insolvency | site | A | `authorization_created` | tls |
| facet | game | A | `new` | tls |
| crowdsize | tool | A | `new` | tls |
| castwell | tool | A | `new` | tls |
| noisewell | tool | A | `authorization_created` | tls |
| metascrub | tool | A | `authorization_created` | tls |
